.feed 
    .feed-header
        .feed-tab 
            each tab, index in ["My Feed", "Popular Feed", "Recent""] 
                .feed-tab-item(class=`${index == 0 ? "is-active": ""`)= tab 
        .feed-toggle
            +toggle
            p New Releases &amp; Guest Only 
    .feed-main 
        .feed-item 